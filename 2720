T = int(input(""))
for i in range(T):
    number = int(input())
    count = 0
    coins = [25, 10, 5, 1]

    for coin in coins:
            count = number // coin
            print(count, end=" ")
            number = number % coin
